export default function Settings() {
  return (
    <div>
      <h2 className="text-2xl font-semibold">Settings</h2>
      <p className="mt-4">App settings, backups and account connections.</p>
    </div>
  );
}
